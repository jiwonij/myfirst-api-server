apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
  name: sk071-myfirst-ingress
spec:
  ingressClassName: public-nginx
  rules:
  - host: sk071-ingress.skala25a.project.skala-ai.com
    http:
      paths:
      - backend:
          service:
            name: sk071-custom-jenkins
            port:
              number: 8080
        path: /jenkins
        pathType: Prefix
      - backend:
          service:
            name: sk071-myfirst-api-server
            port:
              number: 8080
        path: /
        pathType: Prefix
  tls:
  - hosts:
    - 'sk071-ingress.skala25a.project.skala-ai.com'
    secretName: sk071-ingress-project-tls-cert
